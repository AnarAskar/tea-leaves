name = "tea-leaves"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages (free tier, commercial use allowed)
# Dashboard: build command = npm run build, output directory = dist
# Set BOT_TOKEN and CHAT_ID as encrypted environment variables.
